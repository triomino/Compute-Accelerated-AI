#!/bin/bash
# Auto-generated by vllm-pd-deployer
# Description: Proxy/Load Balancer startup script

unset http_proxy
unset https_proxy

python {{ proxy.workdir }}/{{ proxy.script_path }} \
    --port {{ proxy.port }} \
    --host {{ proxy.host }} \
    --prefiller-hosts \
{% for host in prefill_hosts %}
       {{ host }} {% if not loop.last %}\{% endif %}

{% endfor %}
    --prefiller-ports \
{% for port in prefill_ports %}
       {{ port }}{% if not loop.last %} {% endif %}{% if loop.last %} \{% endif %}
{% endfor %}

    --decoder-hosts \
{% for host in decode_hosts %}
       {{ host }} {% if not loop.last %}\{% endif %}

{% endfor %}
    --decoder-ports \
{% for port in decode_ports %}
       {{ port }}{% if not loop.last %} {% endif %}
{% endfor %}
