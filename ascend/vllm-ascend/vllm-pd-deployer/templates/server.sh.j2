#!/bin/bash
# Auto-generated by vllm-pd-deployer
# Node: {{ node.name }}
# Role: {{ role }}
# Description: Main server startup script

python {{ workdir }}/launch_online_dp.py \
    --dp-size {{ dp_size }} \
    --tp-size {{ tp_size }} \
    --dp-size-local {{ node.local_dp_size }} \
    --dp-rank-start {{ node.dp_rank_start }} \
    --dp-address {{ node.dp_master_host }} \
    --dp-rpc-port {{ node.dp_rpc_port }} \
    --vllm-start-port {{ node.vllm_start_port }}
